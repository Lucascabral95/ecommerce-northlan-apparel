# Terraform

Terraform modules and environments are intentionally deferred.

The target architecture is AWS ECS Fargate, RDS PostgreSQL, a managed or self-hosted RabbitMQ option, Redis-compatible cache, ALB, ECR and CloudWatch logs.
