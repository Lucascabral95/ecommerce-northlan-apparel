# Docker

Docker assets will be added after the service foundation stabilizes.

Phase 1 intentionally avoids defining runtime infrastructure. The first Docker deliverable should be a reusable Node service image and a local Compose file once PostgreSQL, RabbitMQ and Redis are introduced.
